const main = React.createElement

function mainPage(){
    return (
      <ons-page>
        <button>Please</button>
      </ons-page>);
}

React.render(main(mainPage),document.getElementById('MainPage'))